{
  "compilerOptions": {
    "target": "ES6",                          // Modern JS output
    "module": "ESNext",                       // Use ESNext modules for better tree-shaking and flexibility
    "moduleResolution": "node",              // Standard for resolving modules
    "sourceMap": true,                        // Enable source maps for debugging
    "noEmit": true,                           // Don't output compiled files (useful with tools like webpack, swc, etc.)
    "removeComments": true,                   // Strip comments from output
    "preserveConstEnums": true,               // Keep const enums as-is
    "strict": true,                           // Enable all strict type-checking options
    "esModuleInterop": true,                  // Better compatibility with CommonJS modules
    "forceConsistentCasingInFileNames": true  // Helps avoid import case sensitivity issues across platforms
  },
  "include": [
    "src/**/*"
  ],
  "exclude": [
    "node_modules",
    ".git",
    "dist",
    "build"
  ],
  "typeAcquisition": {
    "enable": true
  }
}
